<% if message.user == user %>
  <div class="self-end bg-blue-500 max-w-xs md:max-w-md px-4 py-2 rounded-full text-white">
    <%= message.content %>
  </div>
<% else %>
  <div class="self-start bg-gray-300 max-w-xs md:max-w-md px-4 py-2 rounded-full">
    <%= message.content %>
  </div>
<% end %>
